= 2026-07-14 — Release CI, installers, winget/Homebrew

* Added packaging scripts for Windows (ZIP + Inno Setup), macOS (DMG/ZIP), and Linux (tar.gz/AppImage).
* Added GitHub Actions release workflow building x64 and arm64 on Windows, macOS, and Linux.
* Release notes include a download table with SHA-256 checksums.
* Added winget and Homebrew cask publish workflows (require `WINGET_TOKEN` / `HOMEBREW_TAP_TOKEN`).
* Tagged `v0.1.0` as the first packaged release.
