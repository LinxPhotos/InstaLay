= 2026-07-15 — Host InstaLay site on GitHub project Pages

* Storefront lives at *https://linxphotos.github.io/InstaLay/* (`GITHUB_PAGES_BASE=/InstaLay/`).
* Solid Router `base` matches the Vite base so `/InstaLay/` no longer hydrates blank.
* Custom domain `instalay.linx.photos` deferred until DNS exists (then switch base to `/`).
