- **Windows installer fixed** — Start Menu shows **InstaLay** (search: Insta / Lay / Layout); setup no longer ships incomplete builds that crash on missing DLLs.
- Binary renamed to `instalay.exe`; Dart package is `instalay` (brand stays InstaLay).
- Release CI fails closed on broken Flutter Windows packages; builds libjxl locally for x64.
