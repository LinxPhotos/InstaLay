- **Shared project sources** — One source library per project version; every layout/canvas can use the same photos, with checkboxes to include a subset on the active canvas.
- New layouts start with all shared sources included (tapestry capped at Instagram’s 20-photo carousel limit).
- Legacy projects migrate on load; version clone keeps source membership linked across layouts.
