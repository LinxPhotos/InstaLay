- **Source bitmap cache for edit previews** — Decode once on import into mem/disk RGBA cache; settings changes re-frame only and display via `RawImage` (no JPEG encode on the interactive path).
- Export still uses full-resolution decode and the user's chosen codec.
