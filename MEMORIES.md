# Machine / environment facts for InstaLay agents

- Flutter SDK path: `C:\flutter-sdk\flutter\bin` (refresh PATH if `flutter` missing in shell) -- used: 13
- Desktop window size/position/maximized persist via `window_manager` + SharedPreferences (`instalay_window_v1_*`); bootstrap in `lib/desktop/desktop_window_io.dart` -- used: 1
- Whole-UI zoom: `uiScaleProvider` (`instalay_ui_scale_v1`, 0.75–1.5 step 0.1); Ctrl/Cmd+=/−/0 + AppBar `UiScaleButtons`; `UiScaledChild` is **textScaler-only** (no Transform / MediaQuery size rewrite — those overflowed right/bottom on un-maximize and desynced hits). Editor rails shrink when the window is too narrow -- used: 6
- This repo clone: `Z:\code\github.com\LinxPhotos\instalay` (GitHub `LinxPhotos/InstaLay`). Linx org clones: `Z:\code\github.com\LinxPhotos\`. Personal CODE_ROOT projects: `Z:\code\github.com\AMDphreak\` -- used: 4
- After moving/renaming the clone (e.g. `AMDphreak/insta-lay` → `LinxPhotos/instalay`), or changing `BINARY_NAME` / CMake `project()`, wipe `build/windows` (or `flutter clean`) — stale CMakeCache fails with "Unable to generate build files" / `$<TARGET_FILE_DIR:…>`. Diagnose/fix: `.\scripts\ensure_windows_cmake_cache.ps1` / `-Fix`. Spot it: CMake errors mentioning a different directory than cwd, or `CMAKE_HOME_DIRECTORY` inside `build/windows/*/CMakeCache.txt` ≠ this checkout -- used: 4
- Dart package name is `instalay` (`package:instalay/...`); on-disk app data folder `instalay/` with one-time migrate from legacy `insta_lay/` (`lib/services/app_paths.dart`). Visible brand remains **InstaLay** -- used: 1
- PR CI (`.github/workflows/ci.yml`) only runs `flutter analyze` + `flutter test` on Ubuntu — it never compiles Windows desktop. A green CI check does not mean `flutter run -d windows` works locally -- used: 1
- Windows `jxl_ffi` link needs `JXL_STATIC_DEFINE` (+ CMS/THREADS variants) or you get `__imp_Jxl*` LNK2019. Official `jxl-x64-windows-static.7z` can still fail with `__std_min_element_*` / `__std_max_element_*` if MSVC STL ABI ≠ the prebuilt’s toolset (VS 17.14 / 14.44 observed). Prefer `.\scripts\build_libjxl_prebuilt_windows.ps1` before `flutter build windows`. Release CI must fail on nonzero `flutter` exit and refuse exe-only Release folders in `package_windows.ps1` (v0.6–v0.7.3 shipped ~50KB ZIPs / crashing installs) -- used: 2
- Inno Start Menu: display **InstaLay**; search aliases Insta/Lay/Layout via Comment + `System.Keywords` (`scripts/windows/set_start_menu_keywords.ps1`, load .lnk with STGM_READWRITE) + App Paths (`Insta.exe`/`Lay.exe`/`Layout.exe`/`instalay.exe`). Flat `{autoprograms}` + group; wipe old `Insta Lay` folder. Store ASO list remains `store/keywords.txt` -- used: 2
- Windows package manager preference for Node is pnpm; this repo is Dart/Flutter -- used: 1
- Photo decode prefers `dart:ui` `instantiateImageCodecWithSize` (Skia; often libjpeg-turbo under the hood) with `maxLongEdge`. Interactive editing is a **live Skia art canvas** (`LiveFramedCanvas` / `InteractiveTapestryCanvas`) over cached source `ui.Image`s — matte/border/fit/gap/slide divisions repaint instantly. Projects hold multiple **layouts** per version with a shared **Sources** pool (`ProjectVersion.sources`); each layout selects a subset via checkboxes. Instagram carousel max is 20 slides/photos per tapestry **and per batch**; batch preview is a left-aligned LTR strip of all framed photos, with auto-overflow into the next batch when full. Export size slider is **height** (`exportLongEdge` JSON key); width follows frame aspect. Tapestry has optional **tile aspect** (native or preset) + tile fit. `SourceBitmapCache` (mem+disk RGBA) feeds those sources. Export still uses CPU `CanvasRenderer` (Lanczos / package:image) + codec encode. AppBar export is **pan-layout** (all layouts with content); each canvas cell has **per-layout** export. Multi-layout files are prefixed `01_Name_frame_001.ext`. Paper grain is export-only on the live canvas; the Paper texture section has a 100% zoom matte sample (`PaperTexturePreview`) -- used: 10
- JPEG XL: default native **libjxl via `packages/jxl_ffi` submodule** (dart:ffi) on Windows/Linux desktop, using libjxl **v0.12.0 static prebuilts** (lossless + lossy encode/decode). Authoritative package repo: `Z:\code\github.com\AMDphreak\jxl_ffi` (https://github.com/AMDphreak/jxl_ffi). `koni_jxl` remains the fallback when the native DLL/SO is unavailable or fails to load. No GPU JXL path. Windows: prefer local `packages/jxl_ffi/native/prebuilt/windows-x64` via `.\scripts\build_libjxl_prebuilt_windows.ps1` (matches installed MSVC STL). Official `jxl-x64-windows-static.7z` often fails link with `__std_min_element_*` on VS 17.14; needs **7-Zip** only when falling back to that download -- used: 4
- `jxl_ffi` authoritative clone: `Z:\code\github.com\AMDphreak\jxl_ffi`; InstaLay consumes it as git submodule at `packages/jxl_ffi`. Submodule `src/CMakeLists.txt` needs `JXL_STATIC_DEFINE` (+ CMS/THREADS) and C++ linker language on Windows -- used: 2
- Prefer newest pub versions when resolving; migrate call sites for majors (e.g. `FilePicker.pickFiles`, Riverpod 3 `AsyncValue.value`, Adapty 4 `isActivated()`). Flutter SDK still pins some transitives (`meta`, `matcher`, `test_api`) below Latest -- used: 2
- Desktop export: after render, chooser offers Save (primary) + Share; `ExportSave` uses `FilePicker.saveFile` (single) or `getDirectoryPath` + copy (multi-slide). Save and Share never freeze — freezing is explicit *Mark as posted*; *Unlock* clears a mistaken freeze without cloning. AppBar = all layouts; per-cell button = one layout -- used: 3
- `jxl_coder` / `flutter-jxl-coder` evaluated and dropped for InstaLay (Apple-only JPEG↔JXL transcode, not RGBA). Local `.forks/flutter-jxl-coder` removed; GitHub fork kept until any upstream docs PR settles -- used: 2
- Forks clone path: `Z:\code\github.com\AMDphreak\.forks\` -- used: 1
- Adapty is the chosen mobile subscription-management platform (access level `instalay`); desktop stays on IL- keys. Public SDK key via `--dart-define=ADAPTY_PUBLIC_SDK_KEY`. Stripe Checkout must send `customer_user_id` for web→app sync -- used: 1
- FoodTruckNerdz org clones: `Z:\code\github.com\FoodTruckNerdz\` (ftn-app, ftn-site, docs). DevCentr docs: `Z:\code\github.com\dev-centr\docs` + `general-knowledge` -- used: 1
- Linx↔InstaLay bridge: deep links + album variant picker API (no nav merge). Linx photo-service at `Z:\code\github.com\LinxPhotos\photo-service`; docs `INSTALAY-BRIDGE.adoc` -- used: 1
- Brand mark: canonical SVG `assets/branding/instalay_logo.svg` (+ RGBA PNG). Regenerate platform icons with Inkscape export then `python scripts/generate_brand_icons.py` (ImageMagick also available). Home UI uses `flutter_svg` -- used: 1
